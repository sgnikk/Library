<!DOCTYPE html>
<html>
<head>
  <title>Login</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
</head>
<body>
  <div class="container mt-5">
    <div class="card p-4" style="max-width: 400px; margin: 0 auto;">
      <h2 class="text-center">Login</h2>

      <%= form_with(url: login_path, method: :post) do |form| %>
        <div class="mb-3">
          <%= form.label :username, class: "form-label" %>
          <%= form.text_field :username, class: "form-control", required: true, autocomplete: "off" %>
        </div>

        <div class="mb-3">
          <%= form.label :password, class: "form-label" %>
          <%= form.password_field :password, class: "form-control", required: true, autocomplete: "off" %>
        </div>

        <%= form.submit "Log In", class: "btn btn-primary" %>
      <% end %>

      <p class="mt-3 text-center">Don't have an account? <%= link_to "Sign up here", signup_path %></p>
    </div>
  </div>
</body>
</html>
